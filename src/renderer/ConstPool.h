#pragma once

namespace jsr {

    const float c_zero = 0.0f;
    const float c_one = 1.0f;
    const float c_half = 0.5f;
    const float c_two = 2.0f;
    const float c_pi = 3.1415926535f;
    const float c_half_pi = c_pi * c_half;
}